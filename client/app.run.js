'use strict';

angular.module('RoomBaby')
  .run(["$stateParams", "$rootScope", "$state", "$location", "localStorageService", function($stateParams, $rootScope, $state, $location, localStorageService) {}]);
