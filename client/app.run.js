'use strict';

angular.module('RoomBaby')
  .run(["$stateParams", "$rootScope", "$state", "$location", "sessionApi", "localStorageService", function($stateParams, $rootScope, $state, $location, localStorageService) {}]);
